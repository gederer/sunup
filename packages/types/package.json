{
  "name": "@sunup/types",
  "version": "0.1.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./convex": "./src/convex.ts",
    "./domain": "./src/domain.ts"
  },
  "scripts": {
    "lint": "echo 'Linting skipped for @sunup/types - type definitions only'",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@sunup/convex": "workspace:*"
  },
  "devDependencies": {
    "@sunup/test-config": "workspace:*",
    "typescript": "^5"
  }
}
